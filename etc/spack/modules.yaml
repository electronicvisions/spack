modules:
  prefix_inspections:
    bin:
    - PATH
    man:
    - MANPATH
    share/man:
    - MANPATH
    share/aclocal:
    - ACLOCAL_PATH
    share/pkgconfig:
    - PKG_CONFIG_PATH
    lib:
    - LIBRARY_PATH
    - LD_LIBRARY_PATH
    lib64:
    - LIBRARY_PATH
    - LD_LIBRARY_PATH
    include:
    - CPATH
    - C_INCLUDE_PATH
    - CPLUS_INCLUDE_PATH
    lib/pkgconfig:
    - PKG_CONFIG_PATH
    lib64/pkgconfig:
    - PKG_CONFIG_PATH
    ? ''
    : - CMAKE_PREFIX_PATH
  default:
    enable:
    - tcl
